{
  "name": "leave-bot",
  "version": "1.0.0",
  "description": "Notifies a WhatsApp group 60 days before long office breaks (3+ continuous days off) so everyone can book train tickets.",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "start:local": "node --env-file-if-exists=.env src/index.js",
    "dev": "nodemon --env-file-if-exists=.env src/index.js",
    "dry-run": "node --env-file-if-exists=.env src/index.js --dry-run",
    "ingest": "node --env-file-if-exists=.env src/ingest.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "baileys": "6.7.24",
    "express": "^4.19.2",
    "mongoose": "^8.6.0",
    "node-cron": "^3.0.3",
    "pino": "^9.6.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "tesseract.js": "^5.1.1"
  }
}
